name: KH CRM Weekly Report

on:
  schedule:
    - cron: '0 5 * * 1'
  workflow_dispatch:
    inputs:
      date_start:
        description: 'Report start date (YYYY-MM-DD), leave blank for auto'
        required: false
        type: string
      date_end:
        description: 'Report end date (YYYY-MM-DD), leave blank for auto'
        required: false
        type: string
  push:
    paths:
      - .github/workflows/weekly-crm-report.yml

env:
  NODE_VERSION: '20'
  LARK_CLI_NO_PROXY: '1'

jobs:
  generate-report:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          cd scripts
          npm install
          npm install -g @larksuite/cli

      - name: Init Feishu CLI
        run: |
          echo "${{ secrets.FEISHU_APP_SECRET }}" | \
            lark-cli config init \
              --app-id "${{ secrets.FEISHU_APP_ID }}" \
              --app-secret-stdin \
              --brand feishu

      - name: Configure dates
        id: dates
        run: |
          if [ "${{ github.event.inputs.date_start }}" != "" ]; then
            echo "START=${{ github.event.inputs.date_start }}" >> $GITHUB_OUTPUT
            echo "END=${{ github.event.inputs.date_end }}" >> $GITHUB_OUTPUT
          else
            START=$(TZ='Asia/Shanghai' date -d 'last monday -7 days' '+%Y-%m-%d')
            END=$(TZ='Asia/Shanghai' date -d 'last monday -1 days' '+%Y-%m-%d')
            echo "START=$START" >> $GITHUB_OUTPUT
            echo "END=$END" >> $GITHUB_OUTPUT
          fi

      - name: Pull Klaviyo data
        run: |
          cd scripts
          node pull-klaviyo.mjs \
            --start "${{ steps.dates.outputs.START }}" \
            --end "${{ steps.dates.outputs.END }}"
        env:
          KLAVIYO_PRIVATE_KEY: ${{ secrets.KLAVIYO_PRIVATE_KEY }}

      - name: Pull Shopify GMV
        run: |
          cd scripts
          node pull-shopify.mjs \
            --start "${{ steps.dates.outputs.START }}" \
            --end "${{ steps.dates.outputs.END }}"
        env:
          SHOPIFY_STORE: ${{ secrets.KH_SHOPIFY_STORE }}
          SHOPIFY_CLIENT_ID: ${{ secrets.KH_SHOPIFY_CLIENT_ID }}
          SHOPIFY_CLIENT_SECRET: ${{ secrets.KH_SHOPIFY_CLIENT_SECRET }}

      - name: Pull marketing calendar
        run: |
          cd scripts
          node pull-calendar.mjs \
            --start "${{ steps.dates.outputs.START }}" \
            --end "${{ steps.dates.outputs.END }}"
        env:
          FEISHU_APP_ID: ${{ secrets.FEISHU_APP_ID }}
          FEISHU_APP_SECRET: ${{ secrets.FEISHU_APP_SECRET }}

      - name: Generate weekly report
        run: |
          cd scripts
          node generate-report.mjs \
            --start "${{ steps.dates.outputs.START }}" \
            --end "${{ steps.dates.outputs.END }}"

      - name: Publish to Feishu Doc
        run: |
          cd scripts
          REPORT_FILE=$(ls kh-crm-weekly-*.md 2>/dev/null | head -1)
          if [ -z "$REPORT_FILE" ]; then
            echo "No report file found, skipping doc publish"
            exit 0
          fi
          WEEK=$(echo "$REPORT_FILE" | sed 's/kh-crm-weekly-//' | sed 's/\.md//')
          DOC_RESULT=$(lark-cli docs +create \
            --api-version v2 \
            --content "<title>KH CRM Weekly Report - ${WEEK}</title><p></p>" \
            --parent-token KezUf0UI2lR27KdfBK8c4napnUh)
          echo "$DOC_RESULT"
          DOC_ID=$(echo "$DOC_RESULT" | python3 -c "import sys,json; print(json.load(sys.stdin)['data']['document']['document_id'])")
          echo "DOC_ID=$DOC_ID" >> $GITHUB_ENV
          lark-cli docs +update \
            --doc "$DOC_ID" \
            --command append \
            --content @"$REPORT_FILE" \
            --doc-format markdown \
            --api-version v2

      - name: Write to Bitable
        run: |
          cd scripts
          if [ -f "bitable-campaign.json" ]; then
            lark-cli base +record-batch-create \
              --base-token F2pVbsy0OaaocEsQeuYcJKkHnBI \
              --table-id tblladSdv4Aj4Zgm \
              --json @bitable-campaign.json
          fi
          if [ -f "bitable-flow.json" ]; then
            lark-cli base +record-batch-create \
              --base-token F2pVbsy0OaaocEsQeuYcJKkHnBI \
              --table-id tbl2RUsiYrn7zFTq \
              --json @bitable-flow.json
          fi

      - name: Build notification message
        run: |
          python3 scripts/build-notify.py \
            --start "${{ steps.dates.outputs.START }}" \
            --end "${{ steps.dates.outputs.END }}" \
            --doc-id "${{ env.DOC_ID }}"

      - name: Push to operations group
        run: |
          if [ -f "/tmp/kh_msg.md" ]; then
            lark-cli im +messages-send \
              --chat-id "${{ secrets.FEISHU_CHAT_ID }}" \
              --markdown @/tmp/kh_msg.md
          else
            echo "No message file, skipping push"
          fi

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: kh-crm-report-${{ steps.dates.outputs.START }}
          path: scripts/*.md
          retention-days: 30
