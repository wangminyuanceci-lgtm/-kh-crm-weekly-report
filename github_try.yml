name: Minimal f-string Test

on:
  push:
    paths:
      - .github/workflows/try.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: echo test
        run: echo "basic"

      - name: python f-string with expressions
        run: |
          echo "hello"
          python3 -c "
print('test')
"
